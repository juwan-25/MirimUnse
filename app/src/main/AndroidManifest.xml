<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.s2104.mirimunse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MirimUnse">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/background" />

        <activity
            android:name=".LuckCheckDetailActivity"
            android:exported="false" />
        <activity
            android:name=".LuckRecordDetailActivity"
            android:exported="false" />
        <activity
            android:name=".LuckRecordActivity"
            android:exported="false" />
        <activity
            android:name=".LuckCheckActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>